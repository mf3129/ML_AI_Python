import math

radius = float(input("Type in the radius for the circle: "))

area = math.pi * (radius * radius)
circumference = 2 * math.pi * radius

print("The area is", area, "and the circumference is", circumference)
